// Host half of the Sumika brand plugin.
//
// The sidebar brand lives entirely in the browser half; the host plane exists so
// the loader can find this package's `dsh.client` declaration.

export const name = 'sumika-brand';

export function apply() {}
