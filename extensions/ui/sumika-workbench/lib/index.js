// Host half. The workbench additions are pure client UI that reads state the
// framework already provides, so the host plane only exists for the loader to
// find this package's `dsh.client` declaration.

export const name = 'sumika-workbench';

export function apply() {}
