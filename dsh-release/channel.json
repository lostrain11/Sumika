{
  "schema": "sumika.dsh-release-channel/v1",
  "default_release": "0.1.1-rc.2",
  "notes": "The default release is the combination the desktop shell, the setup helpers and Core all validate against. Switching it is a reviewed change: run the isolated verification for the candidate, then edit default_release. SUMIKA_DSH_RELEASE selects another described release for isolated verification only; it never rewrites a user's daily profile or install."
}
