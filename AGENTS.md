# Sumika Next 开发入口

本项目使用 DSH 原生 Web、Agent、工具、Skills 和 MCP。新增核心逻辑保持 Harness 中立；优先复用上游能力。不修改 DSH 上游源码、安装包或 node_modules；新增功能通过原生扩展接口与独立适配中间层实现。

开始任务、压缩上下文后或换模型接手时：

1. 阅读 `docs/project/handoff.json`、`progress.json`、`plan.json`。
2. 需求原文在 `requirements.json`，完整已批准计划在 `approved-plan.md`；摘要不增加授权。
3. 核对 Git 和实际实现、测试证据。未验证内容不能作为完成事实。

原生 Plan 用于规划和用户确认；原生子 Agent 用于独立任务和审查。暂停使用取消后交接，恢复先检查历史和实际 diff，不自动重放未知操作。重试前查看上次操作是否已生效。

阶段成果记录实现内容、验证命令与结果、剩余问题、限制及下一步。更新现有项目记录并运行：

```powershell
python -B -m sumika_next.cli check
python -B -m sumika_next.cli handoff
```

DSH `/compact` 后仍从上述文件核验目标，不依赖摘要保存授权。原始需求、代码、diff、成果正文与模型附言保持边界。P4 通过独立连续性扩展接入自动捕获；启用后使用 continuity_query / continuity_record 及 sumika-continuity Skill。

验证说明见 `runtime/dsh/README.md`、`docs/project/phase-02-acceptance.md`。`.sumika-next/` 是本地运行数据，不上传凭据或原始模型请求。旧源码可能仍留在忽略目录中，新实现不以其为架构基础。

## 已有资产优先（强制）

当任务目标存在**现成资产**时，默认做法是**整合或改造该资产**，不是另写一套平行实现。现成资产包括：设计稿（HTML/CSS/图片/原型）、既有实现、schema、配置、数据文件、脚本、上游插件或库。

动手前必须先做这一步，并把结论写进任务记录：

1. 列出本任务相关的现成资产及其路径（至少查一次工作区，不要凭印象）。
2. 判定：**直接使用 / 改造后使用 / 不适用**。
3. 只有选择"不适用"或"另写"时，必须写明原因，并在开始实现前告知用户；不得先写一套自己的版本，等用户发现再说。

反例（本项目已发生过一次）：用户已提供完整的 UI 设计稿，却按自己的理解另写了一套精简外壳，用户看到成品才发现与设计不符。此后 UI 任务的定义完成条件包含**与设计稿逐屏对照**，而不是"页面能渲染"。

自检问题，回答不上就不算完成：

- 这个任务里最强的现成资产是什么？我用了吗？
- 如果我做错了，用户需要先发现什么？为什么我没先发现？

## 防走岔（强制，2026-09-16 因连续三次偏离加入）

用户已经三次在我"实现得很好"的地方发现偏离：另写一套 UI 外壳而不套用设计稿、在 DSH 里重复加了壳顶栏已有的「能力」入口、又加了一个与工作区树重复的「项目」面板。三次是同一个机制，不是三次失误：

1. **把"技术上可行"当成"需求上正确"**。我用一个面板原型去回答"整个客户端能否都是 DSH 前端"这个设计问题，结论是对的，但产物进了产品——回答一个问题不需要交付一个入口。
2. **用实现回答设计问题**。用户问"是不是该…？"，那是要判断，不是要施工；我先写代码，等于替用户做了产品决定。
3. **没有先对照设计稿**。设计稿屏 2 的侧栏本来就是「项目 ▸ 任务」树，只要动手前看一眼就不会再做一个"项目面板"。`已有资产优先`这条我执行过一次（UI 外壳），之后每次 UI 决策都没有重新执行。

因此，任何**界面**改动（含 DSH 皮肤、客户端插件、外壳页面）动手前必须：

- **先定位设计稿**：写出"设计稿中这块是 X（文件 + 大致位置）"，或明确写"设计稿没有这块，属于新增"。新增项必须先问，不许先做。
- **单一入口 / 单一来源**：同一个能力或屏幕不允许有两个入口。新增任何导航或入口前，先列出它会不会与既有入口重复；重复即否决。
- **原型与产品分离**：验证性代码放 `.sumika-next/` 之类的运行目录，绝不放进 `extensions/ui/*` 交付路径；要进产品必须单独一步并说清"这是产品改动"。
- **可检查、可回归**：把上面的约束写成验收断言（例如 `tools/verify_dsh_panel.mjs` 现在断言 DSH 侧栏不得出现任何 Sumika 面板行），而不是只写在文档里。
- **覆盖表**：设计稿每一屏的元素与实现的对应关系记在 `docs/project/ui-design-coverage.md`，偏离与未做逐条列出，避免"用户先发现"。

回答用户问题与改动产品是两件事：问句先答，答完再问要不要做。
