# 备份与恢复

阶段 0 使用 Git 保存项目记录和源码 checkpoint。提交前运行连续性校验，提交后记录 commit SHA。

项目运行数据的备份恢复列入计划，首版暂不考虑加密。备份不得包含 API Key、Token、Cookie、密码或完整私人聊天；恢复时先复制到隔离目录并核对文件清单，再替换目标数据。

新版分支不删除旧分支。需要回退时使用旧分支提交或独立恢复目录，不覆盖未提交数据。
