# 设计稿 ↔ 实现覆盖表（2026-09-16 建立）

设计稿：`D:\Code\Sumika-UI-Designs\direction-d-hiyori\index.html`（四屏 + 顶栏）。
本表存在的理由：避免"用户先发现偏离"。每做一次界面改动，先在这里写清对应关系；
新增项须有需求依据。本轮 UI v2 已批准将现有能力补入合理位置；该范围内的入口不重复询问，范围外的新功能仍须确认。

## 顶栏（.topbar）

| 设计稿元素 | 实现 | 状态 |
| --- | --- | --- |
| 晴日部室 brand + 版本角标 | `ui/app/index.html` 顶栏 | 照做 |
| 活动室 / 工作台 / 能力 / 设置 导航 | 同上，hash 切换 | 照做（唯一导航） |
| 模型 chip + 后端状态 | `bind.js` 写真实角色模型与连接状态 | 照做 |

## 屏 1 活动室

| 设计稿元素 | 实现 | 状态 |
| --- | --- | --- |
| 部员名册（角色 + 状态 + 强调色） | `bindRoster` 用真实角色重建，标注 Sumika 内置 / 用户导入 | 照做 |
| 新增：既有角色资源管理（UI v2 已授权） | 名册管理内提供显示名、绑定资源、导出、归档；复用独立角色模块，修改前个人目录备份 | 实现；后端与浏览器只读入口验收通过，未改用户真实角色 |
| 新增：资源包恢复（UI v2 已授权） | 同一名册管理内提供 ZIP 路径导入／恢复，校验成功才发布，同 ID 拒绝覆盖 | 后端夹具与浏览器表单验收通过；真实用户资源未执行恢复 |
| 舞台（房间 + 镜头 + 活动） | 设计稿 SVG 陈设 + 角色 VRM 实机渲染 | 照做（陈设仍是设计稿占位图） |
| 三维图层图例 | 数据化：列出真正绑定 VRM 的角色 | 照做 |
| 对话栏（消息 + 输入） | 接真实角色服务、每角色独立会话、失败如实报错 | 照做 |
| 输入框左下 `♪`（设计稿约 722 行） | 同位置改成可操作语音按钮；一次确认录音 5 秒、本地识别后填草稿，不自动发送；复用能力页语音设置和独立麦克风授权 | 产品接线完成；浏览器替身与子进程测试通过，真实设备、朗读和连续轮次未验收 |
| 桌宠小窗 | 只显示真实对话记录，board 屏隐藏 | 偏离（原设计带示例台词） |
| 场景随活动切换 | 未实现 | 未做（P6 计划项） |

## 屏 2 工作台（顶栏以下 = DSH 渲染）

| 设计稿元素 | 实现 | 状态 |
| --- | --- | --- |
| 项目 ▸ 任务 树状侧栏 | **DSH 原生侧栏**（工作区 = 项目，会话 = 任务），行距/圆角/字号/悬停已按设计稿令牌调整 | 结构对应；**行内信息未达设计稿**：项目行缺路径与计数、任务行缺状态点与"状态 · 时间"文案 |
| 侧栏顶部"搜索项目与任务" | DSH 原生搜索（默认折叠为图标，输入框样式已按设计稿调整） | 部分：**常驻搜索框需要插件层改动** |
| 侧栏"新建任务"按钮 | DSH 原生 新会话（已按设计稿改为玫红实心） | 照做 |
| 侧栏顶部品牌 | 隐藏（设计稿侧栏无品牌，词标在顶栏）；折叠按钮保留，轨道态仍显示品牌标记 | 照做（实测 `_brand` display:none、`_toggle` 可见、行高 60→40） |
| 侧栏页脚状态块 | 用户明确要求移除的附加品牌与回壳链接已移除；原生设置入口保留 | 依用户后续修订 |
| 顶部面包屑 + 任务标题 + 状态药丸 | `sumika-workbench` 接管 lineage 与 utilities | 部分照做（面包屑改为工作区归属，避免与原生标题重复） |
| 时间线 · 用户消息气泡 | 皮肤：`_bubble` 按设计稿 `.bub` 改（1px 描边 + 8px 圆角 + 水色浅底） | **实测生效**（computed: border 1px / radius 8px / rgb(233,241,247)） |
| 时间线 · 助手消息 | DSH 原生：助手输出是长文 markdown 段落，**没有气泡容器** | 有意偏离：设计稿 `.cm.agent .bub` 是短对话气泡，把长文塞进气泡反而更差；保留文档式排版 |
| 时间线 · 工具卡 | 皮肤：`_callRow` 3px 绿条 + 10px 圆角 + 纸色底 | **实测生效**（border-left 3px / radius 10px / rgb(255,253,248)） |
| 时间线 · diff 统计 | 皮肤：`_diffStat` 靠右 + muted | 节点存在（count 1），规则已写；样式值未单独采样 |
| 时间线 · diff 正文 / 终端结果 | 皮肤规则已写（`_diffBody` / `_codeBody` / `_terminalBody`） | **未验证**：两次真实工具调用（跑测试、写文件）都没有渲染这两个容器，需要一次展开态或不同的工具输出 |
| 审批卡 | 未做 | **未验证且有原因**：当前权限是「工作区内修改」，工作区内写文件不弹审批；要看到审批卡需要切换权限预设（用户操作）或执行工作区外的操作。`dsh-client-ui-approval` 的语义后缀是 `_card` + `_actionRow`，检测器已按此写好 |
| 时间线 · 状态行（"已思考 / 用时"） | 皮肤：`_turnStatus*` 字号与颜色 | **未验证**（当前会话无该节点） |
| 审批卡（4 统计块 + 确认执行/取消） | DSH 原生审批组件 | 未核对（需真实审批场景） |
| 输入区 | 皮肤：输入卡 1px 描边 + 10px 圆角 + 纸色底、底部控件行虚线分隔、占位符 muted、外边距 0 24px 16px；原生控件（+ / 权限模式 / 模型 / 发送 / 队列）全部保留 | **实测生效**（card: border 1px / radius 10px / rgb(255,253,248)；tools: border-top dashed） |
| 侧栏"已归档 N" | DSH 原生归档 | 未核对 |

**当前实现与边界**：不 fork DSH 前端，不新增重复的能力或项目面板。工作台实际通过页面内 iframe 承载受管 DSH，并使用正式主题与客户端 slot 扩展；没有新开标签，但尚未消除 iframe。以前将「工作台就是 DSH」描述成「已无 iframe」不符合代码事实。

**待用户决策（阻塞侧栏还原）**：设计稿的行内信息（路径、计数、状态点、"状态 · 时间"）与常驻搜索框，无法只靠 CSS 表达。两条路：

**用户已选路线 2（保留原生浏览器，2026-09-16）**，因此侧栏按"皮肤上限"收口，实测结论如下：

| 设计稿元素 | 皮肤能否做到 | 依据 |
| --- | --- | --- |
| 新建任务按钮（玫红实心） | ✅ 已做到 | `_newSession` 是语义后缀 |
| 任务行圆角/悬停/选中态 | ✅ 已做到 | `_sessionRow` / `_selected` |
| 任务行圆角方块状态点 | ⚠️ 仅"有状态时" | 该点由 DSH 按真实状态渲染；无状态的行不画点，避免编造状态 |
| 项目行（圆点 + 名称 + 路径 + 计数 + 折叠箭头） | ❌ 做不到 | 工作区行是基础件 `*.root` span（`_root_1b2ny_3`），无语义后缀可锚；路径/计数 DSH 根本没渲染 |
| 任务行"状态 · 时间"文案 | ❌ 做不到 | DSH 只渲染相对时间，状态词未渲染 |
| 常驻搜索框（"搜索项目与任务…"） | ❌ 不做 | 输入框虽在 DOM 里，但 DSH 只有点击搜索按钮才会进入 search 状态并**抢焦点**；仅用 CSS 显示出来会得到一个点了没反应、打字不搜索的假控件 |
| 侧栏页脚状态块 | ✅ 已做到 | 我们自己的 `sidebar.footer.action` |
| 顶栏/侧栏底色、圆角、滚动条 | ✅ 已做到 | 皮肤令牌 |

结论：皮肤路线能把侧栏带到"配色、按钮、行态、页脚"一致，但**行内信息（路径/计数/状态词）与常驻搜索框必须换渲染方**才能达到设计稿。若后续仍想要这部分，需要回到路线 1（接管 `sidebar.workspaces`）。

1. **接管 `sidebar.workspaces`**（`priority: -1` 顶掉原生 WorkspaceBrowser），由 Sumika 用真实工作区/会话数据渲染设计稿的树 —— 视觉最接近，但必须自己重做搜索、归档、重命名、fork、拖拽排序，否则这些能力会丢。
2. **保留原生浏览器**，只在皮肤层继续微调（当前状态）—— 功能完整，但行内信息与常驻搜索框无法达到设计稿。

在用户选定之前，本项保持"部分照做"，不做进一步改动。

## 屏 3 能力

| 设计稿元素 | 实现 | 状态 |
| --- | --- | --- |
| 能力卡片分组 + 状态标签 | `bindCapabilityScreens` 三组：扩展模块（可开关）/ 就绪视图 / DSH 原生 | 照做（分组语义按真实数据调整） |
| 卡片开关 | 真实写入 `/api/capabilities/toggle` 并回读 | 照做 |
| 右侧详情面板 + 计数 | 点选卡片显示真实状态、provider，计数来自注册表 | 照做 |
| 设计稿的"预留"卡片 | 无对应真实项 | 未做（不编造预留项） |

## 屏 4 设置

| 设计稿元素 | 实现 | 状态 |
| --- | --- | --- |
| 左侧分区导航 | 设计稿静态 | 照做（点击切换未接线） |
| 外观（主题 / 背景 / 界面密度） | 未接线 | **未做**（已标注"原型 · 未接入"并禁用按钮） |
| 模型与连接 | 角色模型读 `/api/state`；工作模型与凭据交由 DSH | 照做（偏离：不复述未经核实的模型名） |
| 能力模块 | 真实总览 + 逐项开关 | 照做 |
| 连接与权限 | MCP 改为"由 DSH 工作台管理" | 偏离（原设计写死的授权数不存在） |
| 数据与存储 | 无假检查点哈希；清除数据按钮禁用 | 偏离（已标注未接入） |
| 右侧版本信息 | 真实 DSH 版本 + 项目路径 | 照做 |

## DSH 原生额外能力（设计稿没有，保留不动）

| 能力 | 处理 |
| --- | --- |
| 会话「轨迹」页 | 保留（设计稿只有对话时间线） |
| 用量 / 用时 / token 行 | 保留（真实计量） |
| 「系统提示词」展开 | 保留 |
| 模型选择 chip、权限模式、复制/反馈按钮 | 保留 |
| 侧栏折叠为图标轨 | 保留（皮肤已按设计稿处理悬停品牌标记） |

规则见 `AGENTS.md`「设计稿是视觉参照，不是删功能授权」：这些项只做样式贴近，不隐藏、不删除。

## 未覆盖 / 待核对

- 屏 4 左侧分区已接线，8 个分区真实点击通过；背景与跨页面主题同步尚未完成。
- 屏 2 审批卡与"已归档"在真实 DSH 场景下的呈现。
- 屏 1 场景资源随活动切换（P6 计划项）。
- DSH 升级后所有视觉断言需要重跑：`tools/verify_workbench_ui.mjs`、`verify_workbench_screen.mjs`、`verify_capability_ui.mjs`、`verify_dsh_panel.mjs`。

## 用户术语修订：工作区 → 项目

按用户明确要求，皮肤层将侧栏项目标题、添加/重命名/删除入口与相应无障碍标签改为「项目」；扩展面包屑与交接提示同步。DSH workspace ID、数据和 API 不变，不批量改写对话或代码。原生 locale registry 不允许重复注册已有语言词典，因此本次使用限定在侧栏和菜单/对话框的皮肤文字映射；上游类名变化需回归。真实浏览器确认侧栏「项目」与「添加项目」按钮标签生效。

| 新增：离线状态提示（UI v2 失败状态范围） | 外壳顶部临时状态条，恢复联网后消失，不另增入口；保留原生工作台和草稿 | 浏览器离线/导航/HTTP恢复三项验收通过 |

外观背景行：已接浏览器本地图片保存与恢复默认；活动室保留角色/陈设，图片不上传。四项存储行为验收通过。夜间配色仍预留。

主题行已接浅色/夜间/跟随系统，使用 DSH 正式主题 API 同步工作台；三项行为验收通过，夜间设置截图已核对。原生浮层与其余暗色细节仍待逐屏检查。

## 用户批准的说明文案精简

屏1聊天栏既有「转到工作台」入口：按用户最新要求仅对本条明确任务意图显示，普通闲聊/项目讨论不提示。复用原交接功能，20条本地判定与浏览器渲染夹具通过；不自动派发、不额外调用模型，复杂表达可能漏判。

后续接线：入口须同时收到本次角色回复的高置信度任务标记和逐字原文证据，并通过保守本地过滤。标记不进入正文/角色历史，不二次请求。缺失标记不提示；后端新版本待正常重启生效，未强制重启以保留当前进程内聊天。真实模型遵循率未验收。

移除正式页面中的图层图例、聊天栏架构备注、设置页设计约定和重复排版说明；原型资产 ui/prototype-d 不改。活动演示标题简化为活动。能力页保留开关/依赖/权限说明，标题改为使用提示；未接入状态保留。不是删除业务功能。24 项管理交互回归通过。

活动室追加清理：技术陈设标签、设计说明及无功能角色B人形移除；实际功能与多人预留状态保留。夜间悬浮控件改用主题变量。模型标签仅显示加载中/失败，不常驻实现术语。

## 设置能力分区去重（用户明确授权）

设计稿 `ui/prototype-d/index.html` 屏4约993/1040行包含能力模块导航和总览；产品按本次用户选择移除该分区及重复列表接线，原型保留。具体能力统一在顶栏能力页管理，连接与权限、通用与启动保留。当前无独立通用能力选项，不新增空设置。`verify_ui_management.mjs` 23项真实浏览器检查通过、无页面错误，覆盖7个设置分区和能力页管理功能；新增断言禁止恢复重复分区。

## 侧栏折叠与悬停修复

参照原型屏2 `ui/prototype-d/index.html:346` 的16px顶部留白、38px主按钮和项目树顺序；保留原生搜索、视图和项目操作。用户明确要求借鉴成熟折叠设计。

核对 shadcn/ui `apps/v4/registry/new-york-v4/ui/sidebar.tsx` 的 SidebarTrigger（ghost/icon、PanelLeftIcon、可访问标签）和 VS Code `src/vs/workbench/browser/actions/layoutActions.ts` 的 ToggleSidebarVisibilityAction（原生状态和可访问通知）。借鉴交互，不复制源码，不新增第二个折叠入口：现有原生按钮位于顶部，展开时在新会话右侧，收窄后在图标列顶部，固定显示面板图标；悬停与焦点样式统一。不移植其他项目快捷键。

删除旧皮肤 top:45% 浮动按钮与渐变标记切换规则，侧栏样式集中到浏览器皮肤扩展，刷新加载，无需重启聊天服务。工具提示使用成对背景/前景主题令牌，修复浅底白字。原生功能、点击处理与权限保持原实现。

`tools/verify_sidebar_geometry.mjs`：1280与810宽度分别连续3次折叠/展开通过；顶部间距及按钮不重叠断言通过；浅色提示实测对比度11.12:1；0页面错误。已查看展开/收窄截图，证据 `.sumika-next/sidebar-verification/`。窄窗口测试须等待原生响应式状态稳定，不能在resize后立即按旧状态点击。未声称完成整体工作台重构。

## 提示词优化预留与窄屏

用户提供WorkBuddy截图并明确授权：现有优化入口移到官方 `conversation.input.right` 工具栏，星光图标、悬停提示尚未接通，aria-disabled且无操作，不改写/发送草稿。旧dock模板按钮移除；设置不再提供无效启用开关，已有配置数据保留。未来模型接入F-003 deferred。原稿未定义窄屏，按此次问题修复：680px以下顶栏自动分行，文字不换行、状态次行，380px以下导航独立行；保留所有入口。20项管理回归通过；360/480/810顶栏边界检查通过，480截图人工核对。证据 `.sumika-next/narrow-header/`，脚本 `tools/verify_narrow_header.mjs`。

## 活动室最近聊天与清空

用户明确要求将已有最近三条/更早历史交互用于活动室聊天栏（原index.html约90行仅气泡历史样式）。复用chat-head增加加载更早、清空聊天，不新增导航。默认3条消息而非3轮，每次向前3条，保留翻页滚动位置；切换角色重新加载，过期响应丢弃。清空确认后仅清当前角色room会话，SQLite恢复副本保留，待交接草稿失效，长期记忆和资源不变。生成期间禁用清空。

21项后端测试通过；tools/verify_room_history.mjs合成8条消息验证3→6→8、取消不清空、确认仅执行一次，全程未清用户聊天。旧日用后端尚未切换：兼容旧列表的最近3条/更早记录展示，清空按钮禁用并提示需更新服务；新清空功能不宣称日用已上线。


既有网页咨询授权窗口：复用设计稿屏4连接与权限（ui/prototype-d/index.html 约1051行），补绑定状态、显式受管标签选择及刷新。无新增导航。真实Edge隔离夹具验证失效/未知、跨源与窗口过滤、保存/撤销后的刷新；不代表实际咨询发送及结果回传已验收。


网页咨询恢复接线：沿用 D 方案设置 → 连接与权限、现有网页咨询授权窗口；窗口内新增已批准恢复操作（打开受管窗口/选择会话打开固定站点），无重复导航。verify_browser_authorization_ui.mjs 验证必须显式选择、提交browser/window身份、未知列表不冒充空列表；日用重载与真实操作待验收。

网页咨询恢复入口已加载日用：真实受管启动、三站固定地址打开、显式绑定均通过，三站ready。验证通过日用Bridge接口；Edge隔离夹具覆盖相应按钮与显式选择行为。

网页咨询授权窗口：按用户明确要求新增「启用网页咨询」总开关，默认关闭；沿用设置表单样式。关闭时站点配置操作禁用并说明保留记录。真实Edge夹具验证关闭、刷新持久状态与操作禁用，日用后端拒绝关闭状态的新操作。

- 设置 / 数据与存储：复用原有记忆表单，memory.enabled 已接线并经真实 Edge 验证可见；自动提取仍默认关闭。没有增加导航，关闭不删除已有记忆。

- 数据与存储及现有角色记忆管理窗口：按已批准可选模型提议需求增加默认关闭开关和待确认列表，展示原话、确认/忽略；没有新导航。真实Edge合成数据交互验证通过。


## 能力设置归属与可理解性（R-117 / R-118 / R-119，当前设计参考）

以下是本轮采用的设计方向，并非后续重构必须照搬的布局规范。后续模型若有更合适的信息架构、分组或交互，可调整或替代；应说明改进理由，保留已有功能、用户数据和真实授权边界。

- 本轮将具体能力的配置、子功能开关和授权归到能力页对应卡片的详情。设置页保留通用/启动、外观、模型连接、通用权限与数据管理；本轮避免在设置页再复制同一份语音或记忆配置。此要求更新R-107关于开关位置的早期约定，独立开关语义不变。
- 将语音识别与语音朗读聚合为一个“语音交互”卡片；识别和朗读仍可分别启停，不因合并展示删除底层能力。输入设备、朗读声音、语音配置放在该卡片详情。
- 麦克风采集作为语音交互详情里的独立授权，不再占用能力卡片。模块安装/启用、用户采集授权、系统麦克风权限和每次执行审批是不同状态。默认不从已有模块enabled推断用户已授权；选择设备或打开详情不得开始采集。
- 长期记忆卡片统一提供：长期记忆开关、规则自动提取、模型提议、检索实现选择及既有记忆管理入口。就绪状态与启用状态分别描述，语义依赖缺失不显示为可用；保留记忆/关系、确认提议、导入导出与恢复功能。
- 记录角色用量属于通用数据设置：说明记录的是token使用历史而非账户余额；关闭停止新增统计，保留历史。
- 自动提取、模型提议、检索方式、用量等选项有简短中文解释，优先悬停提示，也须能键盘聚焦/触屏点击查看。明确收益、是否额外消耗token、开关影响，不用工程术语代替说明。
- 沿用ui/prototype-d/index.html能力页“卡片＋详情”与设置表单资产；不新增重复导航，不删除设计稿未覆盖的已实现功能。前端模型可重做视觉，但不得改动权威数据来源、配置保存、授权、隔离和失败关闭行为。

本轮方案的验收（后续采用其他方案时应随之调整）：设置页不再存在独立语音/记忆配置；能力页只有一个语音交互和一个长期记忆卡片；详情能修改原配置并持久化，语音子功能独立；麦克风默认未授权且后端拒绝未授权采集；提示可悬停/聚焦访问；保留其他能力与用户原数据。实现状态另见desktop-integration-execution.md，不以本条需求声明等同完成。

- 模型设置导航回归：沿用原设置导航与卡片，无新入口。兼容缺少 auxiliary/model_library 的旧 Bridge 响应；真实 Edge 验证三用途分栏与外观往返，未写用户配置。验收：tools/verify_model_settings_ui.mjs。

语音停止恢复：复用 `ui/prototype-d/index.html:722` 输入框语音按钮，无新入口。停止失败保留请求 ID，原按钮提供明确的停止重试，不发起新录音；启动响应未到时取消会等待 ID 后停止一次。Edge 隔离产品模块夹具六项通过，证据 `.sumika-next/speech-ui-2d8178c4-e8b7-4126-a4ef-254ca1999bfd/report.json`。未打开麦克风、未重启日用服务；音频播放与连续语音仍未接通。

能力详情写入失败恢复：复用既有语音详情表单，不新增入口。模块/麦克风授权写入失败后读取服务端值与revision，不假定回滚、不重发；读取失败则不确定态并禁用。设置保存失败也刷新基线并保留输入。隔离Edge实际表单四项验证通过，工具 `tools/verify_capability_write_recovery.mjs`；未修改日用配置，未实测音频。

顶栏连接状态修正：复用 ui/prototype-d/index.html:514 的 .proto，不新增入口。/api/state 成功响应表示本地服务连接；角色 session unknown 不再等同断线。离线/联网事件刷新只读状态，联网后延迟一次确认，旧响应不能覆盖新状态。tools/verify_header_connection.mjs 四项 Edge 检查通过（零写入）；完整隔离工作台回归 .sumika-next/native-approval-ui-da92856e9046463381ddaed7223e2f43/report.json 通过。不是模型就绪或服务持续健康承诺，日用服务未重启。


活动室朗读入口（用户明确批准“加入”）：沿用设计稿 ui/prototype-d/index.html:325 的聊天气泡与 mini-btn 风格，在角色回复下方新增单一“朗读／停止”按钮，用户消息及错误提示不显示。仅点击才请求本地输出，不自动启用语音、不使用麦克风或模型。加载历史保留状态、切换角色及清空聊天先停止、失败保留重试停止；启动结果未知且无 ID 时禁止再次启动。超过 4000 字符不截断，按钮提示限制。Edge 产品渲染函数及模块 8 项检查通过，截图已查看；证据 .sumika-next/playback-ui-b0a67753-500b-40f2-90b5-9b2b5817f2c1/report.json。30 项后端回归通过；未做真实声音硬件验收、日用重启或重建安装包。
