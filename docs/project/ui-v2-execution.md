# UI 还原与既有功能整合 v2

## 当前续做：同次角色生成提供任务候选标记

复用 `extensions/roles/chat.py` 的单次生成和 `ui/server.py` 现有聊天返回/历史，不增加分类模型或二次请求；独立 `task_intent.py` 生成本次随机标记并解析附带元数据。正文先剥离标记再进入语言处理和模型历史。格式不合法、未返回、证据不在本条原文则unknown；不重试修复。此为展示建议而非授权。

UI 不再仅按动作关键词立即展示入口，等待本次响应的 high/task 标记、原文证据及现有保守本地规则都满足才显示。刷新历史沿用该条结果；旧消息没有标记不显示。无自动项目选择或派发。该方案仍有漏判，真实模型的协议遵循率未测；不要宣称语义准确性已经验收。

26 项 `test_role_chat`/`test_ui_server` 测试通过，包括一次生成、元数据不进入角色历史、失败关闭。浏览器20条原文夹具加4类不可信标记、点击原文交接通过，0模型请求。为了保留现有进程内聊天上下文，本轮未重启8765服务；后端需下次正常启动才生效，当前进程不产生新标记。前端新门槛缺失标记时隐藏入口。后续应处理聊天会话的可靠恢复，避免每次服务更新丢上下文。

效果入口：http://127.0.0.1:8765/#room 。本轮实现/夹具验证完成，运行服务接入和真实模型遵循率未验证。

## 当前续做：闲聊任务入口按消息意图显示

用户截图反馈普通问候也出现「转到工作台」。现场原因是 `roomMessageNode` 对所有用户消息无条件追加按钮。资产复用：D稿屏1右侧聊天栏、现有 `bind.js` 消息渲染与 `handoff.js` 草稿交接，改造后使用；不新增入口。该按钮为既有扩展，用户本轮明确收紧显示范围。

新增独立本地 `task-intent.js` 展示判定，仅明确动作加具体对象时显示；闲聊、讨论、否定、假设、引用以及无绑定的模糊指代不提示。只读取本条用户消息，不从角色回复推断任务，不新增模型请求。历史重新渲染同样应用；原始聊天不修改。规则是保守启发式，复杂表达可能漏判，不声称完整语义理解。点击仍为手动草稿交接，不自动执行、不替用户选择项目、不授予权限。

`node tools/verify_ui_task_intent.mjs`：20 条隔离历史夹具逐条验证判定与真实按钮渲染；点击保留原文并进入工作台，未自动跳转或发任务，模型请求为0。后续仍按既有待办继续125%缩放、真实审批及服务端断线验收。效果：http://127.0.0.1:8765/#room （已打开页面需刷新）。

来源：用户批准本对话中的「Sumika UI 还原与既有功能整合计划 v2（冻结版）」并要求执行。完整原文保留在会话；本文件记录执行边界和实际证据，不增加授权。

## 固定范围

- D 方向设计稿改造后复用：`ui/prototype-d/index.html`、`D:/Code/Sumika-UI-Designs/direction-d-hiyori/index.html`。保留四个主导航；不另写外壳。
- DSH 原生组件、主题、profile 和独立扩展改造后复用；不修改上游、安装包或 node_modules。工作台轨迹、用量、用时、权限、模型、系统提示词、工具详情均保留。
- 已有业务能力补合理入口及必要薄桥接。角色资源/记忆/关系在角色管理；模块及定时任务在能力详情；模型/设备/授权/启动在设置；使用定位链接而非重复管理面板。
- 不扩建常驻识屏、完整远程推理、自动角色派发等尚未完成后台。局部实现与实际可用分开标注。
- 用户资源与授权不进入源码；配置合并保留旧字段，破坏性操作先有可恢复副本；未知执行不重放。

## 包与状态

| 包 | 内容 | 状态 |
| --- | --- | --- |
| UI-1 | 视口、滚动、假数据清理、独立错误状态与入口映射 | in_progress |
| UI-2 | 正式 theme.overrideTokens、折叠、工作台原生能力、提示词预览 | in_progress |
| UI-3 | 名册、角色管理、记忆关系、角色用量、手动草稿交接 | in_progress |
| UI-4 | 能力/设置接线、定时任务、授权设备及启动 | in_progress |
| UI-5 | 四页截图、真实交互、错误/恢复与记录整合 | in_progress |

## 已确认基线与问题

- 当前外壳通过页面内 frame 显示 DSH；不新开标签。保留此运行结构并统一视觉，不将其描述为已消除 frame。
- 原型 body 固定 1440px 宽，窄窗口右栏被裁切；活动按钮引用已移除 speechBub。
- DSH rc.2 ThemeRuntime 提供 overrideTokens/source disposer。旧文档「没有通用主题变量」错误。
- 旧折叠验收点击隐藏控件并吞异常；旧 frameSrc 证据可能包含认证参数，必须脱敏。
- 开始时服务未运行；本轮已启动 8765 外壳与受管 5175 DSH，并生成本轮验收记录。
- 修改前相关文件及已有 diff 备份：`.sumika-next/backups/ui-v2-start/`。有大量既有未提交文件，不全仓暂存或回退。

## 门槛

每包记录实际修改、测试命令、结果、限制与下一入口。四页在 1440x900、1280x720、1160x800 和缩放下对照；实际点击/滚动，不脚本点击隐藏节点。证据仅落本地运行目录，URL 脱敏。更新记录后运行 `python -B -m sumika_next.cli check` 和 `handoff`。

## 当前实现和证据

- UI-1：复用 D 原型，响应式布局修复窄视口裁切；清除主要模拟名册、对话与工作台内容。页面初始化独立报错。仍需清理残余原型文案与逐控件检查。
- UI-2：正式主题覆盖与原生侧栏实际点击通过；提示词本地模板预览、取消和应用通过，不发送模型请求。尚缺独立开关、附件与冲突完整验收，复杂工具/审批/轨迹状态尚待验收。
- UI-3：角色作用域绑定的记忆、关系、导入导出、重置与备份；角色用量展示；乱序角色请求不会覆盖选中的角色。完整角色资源管理（重命名、删除、资源包管理）仍未合并。
- 手动交接：用户消息的「转到工作台」进入现有 DSH input dock；复用原生工作区登记与 setDraft 契约，明确当前目标后加入空白草稿。原文不变，无角色意见混入，不读取额外项目代码、不自动发送。来源 ID 为当前页面生成的引用，未声称是持久聊天记录 ID；待交接内容仅在页面内存，刷新会丢失。扩展层既有 `extensions/roles/handoff.py` 的原文/事实/意见边界继续适用，此 UI 仅处理手动路径，未接自动意图派发或任务完成回传。
- UI-4：八个设置分区接线；设置合并与 revision 冲突检查、记忆作用域检查；模块排序/移除/重新添加，移除标记防止重启复活；设置仅定位到能力页，不重复提供模块开关。定时任务定义/历史入口与网页读/发送授权入口已接；未绑定执行器的任务不会执行，登录状态不从授权推断。
- 来源检查：新交接通信同时验证 origin 与 frame source；原生输入非空或带附件时不覆盖。新 management 写接口检查 Host/Origin/CSRF；旧接口尚未全部迁移，不宣称全站安全边界已统一。

本轮验证：

1. `python -B -m unittest tests_next.test_ui_management tests_next.test_capability_schedule tests_next.test_ui_schedule tests_next.test_ui_server -q`：21 项通过。
2. `node tools/verify_ui_management.mjs`：18 项真实浏览器检查通过，含设置定位、角色乱序夹具、原生增强应用、伪造来源拒绝、原生目标任务交接。没有模型调用；写入的验收草稿已清空。
3. `node tools/verify_ui_v2.mjs`：4 页 × 3 种视口边界检查及正式主题/侧栏点击通过；截图在 `.sumika-next/evidence/ui-v2/`。这不是逐像素完成证明，125% 缩放未验收。

## 下一入口和限制

下一步先完成角色资源管理和提示词增强开关，再做复杂非空 DSH 会话、审批/工具/diff/轨迹、125% 缩放和离线恢复验收；随后补背景/主题同步、设备状态与浏览器登录入口。网页授权目前仅映射三个规范域名，历史别名记录未完成兼容核查。

各包保持 in_progress，不能将本轮测试通过等同于整个冻结计划完成。现有大量未提交修改保持原状；本轮未推送。回退需根据起始备份与本轮文件差异逐项恢复，不能全仓 reset 或删除个人运行数据。

## 续做：角色资源与增强开关

现成资产：`extensions/roles/roles.py` 的改名/绑定/导出直接复用；`ui/app/management.js` 的名册管理改造后复用；D 稿活动室名册位置不变。资源操作是 UI v2 已授权的新增管理内容，不增主导航。

- 用户角色支持修改客户端显示名、绑定 2D/3D/语音/场景资源、导出 zip，以及从名册移除并归档。修改前 zip 备份，移除时保留原目录；当前使用中角色拒绝移除，自带资源只读，链接目录和校验失败资源拒绝处理。个人备份在设置文件旁的 `backups/roles`，不进入项目源码。记忆不随资源移除。
- 导出资源包以本机路径回执呈现；尚无资源包恢复/导入统一对话框，已有角色卡导入入口保留。没有用真实用户角色执行破坏性验收。
- `prompt_enhancement.enabled` 配置持久化，设置的模型分区提供开关。父页面通过受来源检查的消息同步到 DSH 插件；未知/读取失败默认不显示增强入口。关闭会关闭增强预览且不改现有草稿。适用于当前 Sumika 外壳中的工作台；直接打开 DSH 时无宿主配置，不显示此扩展入口。
- 修复设备枚举缺项时下拉框丢失已保存设备索引的问题：保留值并标记未检测到，保存无关设置不隐式清空设备。
- 验证：管理/服务/模型扩展 22 项 Python 测试通过；管理浏览器验收增至 20 项，通过资源控制入口、增强关闭并恢复。浏览器检查恢复了开关原值，没有调用模型。首次组合测试夹具导入曾出现 WinError 5；隔离管理测试与最终组合测试均通过，未将其归因于业务逻辑。

下一步更新为：统一资源包恢复入口，增强附件/草稿冲突验收，复杂 DSH 原生状态、125% 缩放和离线恢复。整个 UI v2 仍在实施。

## 续做：资源包恢复与失败分支

- 资产复用：`extensions/roles/roles.py::import_package` 改造接线到名册管理的 ZIP 导入／恢复对话框。先在用户资源仓库旁的 staging 校验，再发布到个人角色仓库；不覆盖同 ID 角色，不自动激活，不修改记忆或聊天。已归档角色可以从导出 ZIP 恢复；如原角色仍存在，需要先核对并归档，界面不提供静默覆盖。
- 后端夹具验证资源包恢复、重复导入拒绝、损坏包不进入名册与 staging 清理；管理测试 9 项通过。用户真实角色仅检查界面，未用来执行归档或恢复测试。
- `verify_ui_management.mjs` 增加导入表单、增强预览过期保护，以及 DSH 未就绪→其他页面可用→就绪后挂载的浏览器夹具，共 23 项通过。该故障测试模拟 API 的 embed_ready=false，不等同于真实运行中连接中断或进程崩溃恢复。本次草稿交接走到未知工作区拒绝分支，未将其记成目标绑定成功；此前目标绑定成功证据仍只代表此前运行。
- 无模型请求；验收临时草稿清空，增强开关恢复原值。待做：附件保护、运行中断线、复杂 DSH 工具/审批/轨迹状态、125% 缩放和剩余外观接线。整个 UI v2 未完成。

最终组合回归限制：23 项 Python 测试中 2 项在 setUp 的既有 import_card → import_package → temp.replace(destination) 处发生 WinError 5。单独管理套件此前 9 项通过；本轮组合回归未全过。不能认定具体原因是杀毒或业务逻辑，后续需针对 Windows 目录发布失败采证，禁止无限重试掩盖。

## 续做：Windows 发布失败与原生历史 UI

隔离诊断（`.sumika-next/rename-diagnostic/result.json`）：40 次 replace 中 2 次 WinError 5，源目录存在、目标不存在；40 次 rename 无失败。不能据此确定系统内部占用来源。角色发布本来只允许新增，已将 `temp.replace` 改为 `temp.rename`，不要求覆盖、不自动重试；并发目标冲突夹具验证失败时保留对方文件。角色卡 ZIP 临时目录改到仓库所在盘，避免 C 盘 TEMP。

修复后管理/服务/模型/资源安全组合 27 项通过；随后新增并发冲突测试，资源安全 5 项通过。未修改用户角色、未禁用安全软件或放宽权限。

新增 `tools/verify_ui_native_history.mjs`：仅打开已有历史，1280x720 与 1024x576 的对话、轨迹、用量入口点击与权限控件边界通过，截图已查看。工具摘要点击已执行，但详情内容没有断言，不记作完整工具详情验收。1024x576 是缩小 CSS 视口，不冒充真实浏览器 125% 缩放。未发新任务、未审批、未重放工具。

下一步：工具详情/附件保护/待审批状态、真实缩放与运行中断线，随后剩余外观设置。UI v2 仍未完成。

## 续做：附件与工具详情

原生附件夹具使用生成的 1px PNG，仅加入草稿后移除，未发送模型：增强按钮在带附件时禁用，原文保持不变。管理浏览器检查 24 项通过。历史会话检查 8 项通过，文件写入与 Pwsh 终端详情实际点击展开；第一次终端定位失败是选择器未考虑无障碍名称空格，改用工具名后通过，未改上游行为。详情截图保留在本地证据目录。

截图审查发现额外问题：1024x576 下滚动到工具详情时，侧栏出现横向偏移裁切；目前的边界断言仅覆盖权限控件，不能证明整个工作台无裁切。此问题加入下一项修复，不标为全布局通过。另补齐「选择工作区」按钮辅助标签为「选择项目」，其余底层 workspace 标识不变。

下一步优先定位详情滚动的横向裁切；随后待审批状态、真实缩放、运行中断线与外观设置。无新模型请求、无审批和工具重放。

## 修复工具详情导致的横向裁切

实测定位：1024x576 下外层 DSH `*_frame` 的 scrollWidth=1369、scrollLeft=136，侧栏 x=−136；该层 overflow:hidden 仍允许 scrollIntoView 隐式滚动。皮肤限定在含原生侧栏的外层 frame 设置 overflow:clip，保留内部滚动容器，不修改 DSH 上游。新增断言工具详情聚焦后外层 scrollLeft=0、侧栏 x=0；9 项历史工作台检查通过，修复截图已人工查看。

用户新增交付要求：每次完成附可点击的本地效果链接。本机工作台 http://127.0.0.1:8765/#board；活动室 #room、能力 #shelf、设置 #settings。这些是本地服务地址，不是公开部署链接。

## 浏览器离线恢复

复用现有外壳与原生 DSH 重连机制，不增加导航或任务重试。新增外壳状态提示：navigator.offline 时提示保留页面及草稿，online 时移除提示；不卸载 iframe、不自动重发。

`node tools/verify_ui_offline.mjs`：浏览器上下文真实置离线，HTTP 请求失败；切换 room/board 后 iframe 身份与未发送草稿保持；恢复联网后 HTTP 成功、草稿保持。3 项通过，验收草稿清空，未调用模型。该证据不涵盖服务进程崩溃、执行中断、WebSocket 任务重放或待审批恢复，不能作为这些场景的完成证明。

预览：http://127.0.0.1:8765/#board 。下一步仍为待审批状态、真实浏览器缩放、服务端断线恢复和外观设置。

## 外观：本地背景

复用 D 稿设置页的背景行与活动室 stage，只替换底层背景图，保留角色与陈设，不新增导航。`ui/app/appearance.js` 用 IndexedDB 保存浏览器个人图片，不向服务端上传；只接受 PNG/JPEG/WebP，限制 12 MiB 和 3200 万像素，解码/保存失败不改变原背景。恢复部室午后清除保存项。存储属于当前浏览器与 origin，清除浏览器数据会丢失；不是用户角色资源包的一部分。夜间主题仍未接线。

`node tools/verify_ui_background.mjs` 四项通过：保存、刷新恢复、无效图片保留原背景、恢复默认后刷新。首个测试 PNG 不能解码，改为浏览器 canvas 生成有效 PNG 后通过；无效图片另做明确断言。测试用隔离浏览器上下文，没有修改用户背景。工作台审批/缩放/服务端断线仍未验收。

效果入口：http://127.0.0.1:8765/#settings → 外观；http://127.0.0.1:8765/#room 查看背景。

## 外观：浅色／夜间／跟随系统

复用 D 稿主题行和现有 CSS tokens，`ui/app/theme.js` 管理外壳选择；DSH 薄插件通过正式 ThemeRuntime.setTheme 接口同步，不改上游文件。跨 frame 消息验证来源窗口与 origin。首次无外壳偏好时采用 DSH 当前偏好，之后保存的外壳选择随工作台挂载同步；原生主题变化也回报外壳。自定义背景和角色模型不套滤镜。存储范围为当前浏览器，DSH 原生偏好由其自身 profile 保存。

`node tools/verify_ui_theme.mjs`：夜间外壳/原生工作台同步、刷新保留、系统明暗变化响应，3 项通过；已查看夜间设置页截图并修正模型标签的浅色底。验收结束恢复原主题。未声称所有原生浮层及角色陈设已完成夜间视觉验收，后续仍需逐屏核对。

预览：http://127.0.0.1:8765/#settings → 外观 → 主题配色。切换即时生效。

用户要求精简设计说明：已清理活动室图层图例、聊天架构备注、设置页重复说明及设计约定，移除对应无用图例绑定；不删权限说明或功能。24 项浏览器交互回归通过。预览：http://127.0.0.1:8765/#room 与 #settings。

## 夜间活动室与残余设计标注

逐屏截图发现活动室控件使用硬编码浅色背景，夜间浅色文字不可读；已用现有配色变量修正视角按钮、状态提示与活动框，保留浅色房间与真实角色材质。删除房间设计说明、桌椅技术标签及无功能的角色B占位人形；多人同屏预留状态与真实名册保留。模型成功加载后去掉技术标签，加载中/失败信息保留；标签改 textContent 构造，避免把用户角色名称当 HTML。

主题三项与管理24项回归通过，夜间活动室/能力/工作台截图已生成并检查活动室与能力页。最后移除静态占位人形为低影响展示清理，未单独重跑全套。整个 UI v2 尚未完成；待审批、真实缩放和服务端断线仍需验收。

效果：http://127.0.0.1:8765/#room ；夜间主题在 #settings → 外观切换。

## 隔离原生审批组件验收

复用已安装 DSH 原生 ApprovalPanel，新增 `tools/verify_ui_approval_fixture.mjs`，仅在隔离浏览器上下文拦截加载以取得 React，给组件传入内存测试 pending 对象，不调用 approval/request、不创建真实授权。固定测试布局参数为详情高度200px/内容宽680px。三项通过：长详情滚动与操作可见、拒绝/允许一次返回不同结果、提交失败恢复按钮。

截图发现夜间拒绝按钮低对比度，皮肤增加局部原生颜色变量覆盖；后续重载验证已完成，详见下节。此证据只覆盖组件，不能替代真实审批队列、工具授权或服务器重连验收。生产页面无测试卡，也未执行工具。

下一步先重载核验审批配色和真实嵌入布局，再继续缩放与服务端断线。预览：http://127.0.0.1:8765/#board 。

## 审批配色与主题事件时序修复

资产：D 稿 `ui/prototype-d/index.html` 屏2工作台及审批卡为视觉参照；现有 `sumika-skin/client.js` 与原生 ApprovalPanel 改造后复用，不新增入口，不修改上游。

实测旧版切换 light→dark 时，状态标记已为 dark，但 body 的背景 alias token 仍为浅色 #fffdf8；切回 light 又落到原生 #fff。上游 ThemeRuntime.publish 同步分发快照，皮肤在回调中 overrideTokens/dispose 会嵌套 publish，外层旧快照随后覆盖新颜色。皮肤改为合并队列中的微任务同步，待当前通知分发结束再更新 override，并在卸载时停用队列。没有改变主题选择和授权语义。

重载 DSH 后 `verify_ui_approval_fixture.mjs` 6 项通过：长详情滚动/操作可见、两种决定隔离、失败恢复，加 light→dark→light 实际背景及拒绝按钮对比度 ≥4.5。浅/深截图已查看，背景分别为 #fffdf8 / #151517，消除混色。`verify_ui_theme.mjs` 3 项通过（同步、刷新保留、跟随系统），测试恢复原主题。无模型请求、真实审批和工具调用。

仍待真实审批队列与实际嵌入状态、浏览器125%缩放、服务端断线恢复；UI v2 仍 in_progress。效果入口：http://127.0.0.1:8765/#settings → 外观；工作台 #board。
